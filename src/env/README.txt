This type checked env structure here is copied from the https://env.t3.gg/docs/nextjs page.

These two files are imported by the next.config.ts file which causes the shape of the env object to be validated at build time. 